package comp5703.sydney.edu.au.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import comp5703.sydney.edu.au.kinderfoodfinder.ProductDatabase.AccEntity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ACC_ENTITY".
*/
public class AccEntityDao extends AbstractDao<AccEntity, Long> {

    public static final String TABLENAME = "ACC_ENTITY";

    /**
     * Properties of entity AccEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, long.class, "id", true, "_id");
        public final static Property Sid = new Property(1, String.class, "sid", false, "SID");
        public final static Property ParentId = new Property(2, String.class, "parentId", false, "PARENT_ID");
        public final static Property Accreditation = new Property(3, String.class, "accreditation", false, "ACCREDITATION");
        public final static Property Rating = new Property(4, String.class, "rating", false, "RATING");
    }


    public AccEntityDao(DaoConfig config) {
        super(config);
    }
    
    public AccEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ACC_ENTITY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: id
                "\"SID\" TEXT," + // 1: sid
                "\"PARENT_ID\" TEXT," + // 2: parentId
                "\"ACCREDITATION\" TEXT," + // 3: accreditation
                "\"RATING\" TEXT);"); // 4: rating
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_ACC_ENTITY_SID ON \"ACC_ENTITY\"" +
                " (\"SID\" ASC);");
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ACC_ENTITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, AccEntity entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
 
        String sid = entity.getSid();
        if (sid != null) {
            stmt.bindString(2, sid);
        }
 
        String parentId = entity.getParentId();
        if (parentId != null) {
            stmt.bindString(3, parentId);
        }
 
        String accreditation = entity.getAccreditation();
        if (accreditation != null) {
            stmt.bindString(4, accreditation);
        }
 
        String rating = entity.getRating();
        if (rating != null) {
            stmt.bindString(5, rating);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, AccEntity entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
 
        String sid = entity.getSid();
        if (sid != null) {
            stmt.bindString(2, sid);
        }
 
        String parentId = entity.getParentId();
        if (parentId != null) {
            stmt.bindString(3, parentId);
        }
 
        String accreditation = entity.getAccreditation();
        if (accreditation != null) {
            stmt.bindString(4, accreditation);
        }
 
        String rating = entity.getRating();
        if (rating != null) {
            stmt.bindString(5, rating);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    @Override
    public AccEntity readEntity(Cursor cursor, int offset) {
        AccEntity entity = new AccEntity( //
            cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // sid
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // parentId
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // accreditation
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // rating
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, AccEntity entity, int offset) {
        entity.setId(cursor.getLong(offset + 0));
        entity.setSid(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setParentId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setAccreditation(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setRating(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(AccEntity entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(AccEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(AccEntity entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
